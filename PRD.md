# 예능 기획안 Pre-viz 서비스 기획서 (PRD)

- 작성자: [ ]
- 작성일: (오늘 날짜)

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

한국 방송사의 예능 프로그램 PD는 새로운 예능 기획 아이디어를 제안할 때, 실제 영상 제작에 들어가기 전까지 아이디어를 텍스트로만 설명하고 판단해야 한다. 이 때문에 기획 회의나 보고 과정에서 아이디어가 실제로 어떤 그림으로 구현될지 서로 다르게 상상하게 되고, 이를 시각적으로 보여줄 방법이 없어 커뮤니케이션에 어려움을 겪는다.

---

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

현재는 PD가 기획안을 텍스트(기획서, 구두 설명)로만 작성·전달하고, 필요할 경우 별도로 참고 영상이나 레퍼런스 이미지를 직접 찾아 첨부하는 방식에 의존하고 있다. 이 방식은 다음과 같은 한계가 있다.

- 텍스트만으로는 실제 화면 구성(인물, 배경, 카메라 구도, 호흡)을 구체적으로 전달하기 어렵다.
- 레퍼런스를 사람이 직접 찾고 정리하는 데 시간이 걸리고, 기획 의도와 완전히 일치하지 않는 경우가 많다.
- 초기 아이디어 단계에서 시각화 없이 판단하다 보니, 제작 착수 이후에야 방향이 잘못됐음을 알게 되는 경우가 있다.

---

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 목표: 예능 기획안을 영상(프리비주얼라이제이션)으로 사전에 확인함으로써, 텍스트만으로 판단하던 기존 방식보다 **기획안 사전 검토의 정확도와 효율을 개선**한다.
- 성공 기준:
  - 기획 아이디어 입력 후 12개 프레임 텍스트 스토리보드 생성까지 걸리는 시간 **1분 이내**
  - 스토리보드 1건당 평균 재생성 요청 **2건 이내**로 원하는 결과에 도달

---

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

**사용자:** 한국 방송사 예능 프로그램 PD (개인 사용)

**이용 흐름:**
예능 기획 아이디어 텍스트 입력 → AI가 12개 프레임 텍스트 스토리보드 자동 생성 → PD가 프레임별로 검토 → 마음에 들지 않는 프레임만 선택해 개별 수정/재생성 (분량은 PD가 직접 수동으로 조정 가능) → 12개 프레임 확정 → (이 앱의 역할은 여기까지) PD가 완성된 텍스트 스토리보드를 구글 Flow Omni에 직접 입력해 이미지 생성·확인 → 영상(프레임당 2~10초, 짝수초)으로 제작

---

## 5. 주요 기능 (must / nice로 구분)

### must
**1) 예능 기획 아이디어 → 12프레임 텍스트 스토리보드 자동 생성**
- 설명: PD가 예능 기획 아이디어를 텍스트로 입력하면, AI가 이를 12개의 프레임(시퀀스)으로 구성된 텍스트 스토리보드로 변환해준다.
- AI가 꼭 지켜야 할 규칙:
  - 프레임 수는 정확히 12개로 고정하며, 순서는 임의가 아니라 서사(기승전결 등 이야기 흐름)에 맞게 1~12번을 배치한다.
  - 생성 결과의 순서가 서사 흐름과 맞지 않을 경우, AI가 스스로 재검토하여 서사에 맞게 재배치한다.
  - 기획 아이디어 입력은 최소 50자, 최대 500자 범위 내에서만 받으며, 범위를 벗어나면 생성 요청을 막는다.
  - 입력한 기획 아이디어가 (50자 이상이더라도) 정보가 부족하면, AI가 부족한 부분을 스스로 보완해 12개 프레임 스토리보드를 제안한다.
  - 각 프레임 설명은 최소 100자 이상으로 작성한다. 글자 수 상한은 없으나, 길어지더라도 구글 Flow Omni가 그대로 이미지 생성 프롬프트로 명확히 인식할 수 있도록 구체적이고 명료하게(인물/배경/행동/카메라 구도 등) 작성한다.
  - 각 프레임의 영상 길이는 2초~10초 사이의 짝수(2/4/6/8/10초) 중 하나이며, AI가 스토리보드 내용에 따라 적정 분량을 제안하되 PD가 이 값을 직접 수동으로 변경할 수 있다.

**2) 프레임 개별 수정/재생성**
- 설명: PD가 12개 프레임 중 마음에 들지 않는 프레임만 골라 수정 요청하면, 해당 프레임만 다시 생성해준다.
- AI가 꼭 지켜야 할 규칙:
  - 특정 프레임을 재생성할 때 나머지 8개 프레임 내용은 변경하지 않는다.
  - 재생성된 프레임도 최소 100자 이상 묘사, 2~10초 짝수 분량 제안(PD가 수동 변경 가능) 조건을 동일하게 지킨다.
  - 프레임 순서(1~12)와 번호는 그대로 유지한다.
  - 동일 프레임에 대한 재생성 요청은 최대 10회까지만 허용한다.
  - 재생성 직전 상태는 임시로 보관해, PD가 원하면 방금 재생성한 프레임을 바로 이전 버전으로 1단계 되돌릴 수 있다 (버전 이력 관리는 아니며, 가장 최근 1단계만 지원).

**공통 규칙**
- 스토리보드 생성/재생성 중 API 오류나 네트워크 장애가 발생하면 1~2초 간격을 두고 최대 3회까지 자동으로 재시도한다.
- 3회 자동 재시도까지 모두 실패하면, 에러 메시지와 함께 PD가 직접 누를 수 있는 "재시도" 버튼을 보여준다.

### nice
- 이번 범위에서는 없음 (전부 must 기능으로만 구성)

---

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

**범위 (만들 것)**
- 예능 기획 아이디어 → 12프레임 텍스트 스토리보드 자동 생성
- 프레임 개별 수정/재생성
- 새로고침은 물론 브라우저를 껐다가 다시 열어도, 마지막으로 작업하던 스토리보드가 그대로 남아있다 (브라우저 localStorage 기반, 버전 이력 관리와는 별개)
- Vercel 배포 후 외부에서 URL로 접근해도, 공유 비밀번호(PIN)를 입력해야 화면을 볼 수 있다.
- 완성된 스토리보드는 화면에 항상 텍스트로 표시되어 직접 드래그해서 복사할 수 있고, 프레임별/전체 복사 버튼과 텍스트 파일(.txt) 다운로드로도 내보낼 수 있다 (구글 Flow Omni에 옮겨 붙일 때 사용).

**비범위 (만들지 않을 것)**
- 실제 이미지/영상 생성 기능은 만들지 않는다 — 이미지·영상 제작은 구글 Flow Omni에서 별도로 진행하며, 이 앱은 텍스트 스토리보드까지만 담당한다.
- 구글 Flow Omni와의 API 연동은 만들지 않는다 — PD가 완성된 텍스트 스토리보드를 Flow Omni에 직접 수동으로 입력한다.
- 기획안 저장 이력 관리(버전 관리 등)는 만들지 않는다 — 이번엔 2개 핵심 기능에만 집중하고, 다음 버전 과제로 남긴다.
- 다중 사용자(협업/공유/댓글) 기능은 만들지 않는다 — PD 개인 사용에 한정하며, 다음 버전 과제로 남긴다.
- 계정별 로그인/회원가입 기능은 만들지 않는다 — 개인별 계정 없이, 공유 비밀번호(PIN) 하나로 접근을 제한하는 간단한 게이트만 둔다.
- 민감 소재(선정성·폭력성 등) 콘텐츠 필터링 기능은 만들지 않는다.

---

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- **공개등급:** 방송사 내부(사내) 업무용 도구로, 외부 비공개를 원칙으로 한다.
- **개인정보 여부:** 입력값은 예능 기획 아이디어 텍스트가 기본이며, 실존 인물의 이름·연락처 등 개인정보는 입력받지 않는 것을 원칙으로 한다. 향후 캐스팅 관련 정보 등이 포함될 경우 별도 검토가 필요하다.
- **API 키 관리:** 스토리보드 텍스트 생성에 OpenAI API(`OPENAI_API_KEY`)를 사용한다. 키는 `.env` 파일에 저장하고 `.gitignore`에 `.env`를 등록해 GitHub 등 외부 저장소에 올라가지 않도록 관리한다. (구글 Flow Omni는 API 연동 없이 PD가 수동으로 입력)
- **인증:** 계정별 로그인은 없으며, 공유 비밀번호(PIN) 1개로 접근을 제한하는 간단한 게이트만 둔다. 비밀번호도 `.env`에 저장해 관리한다.

---

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- **Next.js** — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- **고정 이유:** Day 1 실습에서 쓴 것과 같은 구성이고, 3일차에 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함
- **OpenAI API** — 텍스트 스토리보드 생성을 위한 AI 모델 호출 (`OPENAI_API_KEY` 사용)

---

## 개발 단위

1. Next.js 프로젝트 기본 구조 세팅 (페이지/레이아웃 생성)
2. 기획 아이디어 입력 UI(텍스트 입력창 + 생성 버튼, 50~500자 글자 수 제한/카운터) 구현
3. 스토리보드 생성 API 라우트 구현 (입력 텍스트 → AI 호출 → 결과 반환)
4. AI 프롬프트 설계 — 12프레임 고정, 서사 순서, 100자 이상 서술, 2~10초 짝수 분량 제안 규칙 반영
5. 생성된 12개 프레임을 카드/리스트 형태로 화면에 표시
6. 프레임별 분량(초) 수동 수정 UI 구현
7. 프레임 개별 재생성 요청 UI 및 API 라우트 구현
8. 프레임별 재생성 요청 횟수 제한(최대 10회) 로직 구현
9. API 오류·네트워크 장애 시 1~2초 간격, 최대 3회 자동 재시도 + 실패 시 에러 메시지·수동 재시도 버튼 UI 구현
10. localStorage 기반으로 브라우저를 껐다 켜도 작업 내용이 유지되도록 상태 저장 구현
11. 전체 UI 스타일링 적용 (화이트 배경 + ENA Blue 포인트 + KT Flow 서체 파일 포함, 최소 1440px 데스크톱 모니터 기준 PC 전용 레이아웃)
12. 공유 비밀번호(PIN) 입력 게이트 구현 (미입력/오답 시 화면 접근 차단)
13. 스토리보드 프레임별/전체 복사 버튼 및 텍스트 파일(.txt) 다운로드 기능 구현
14. 프레임 재생성 직전 상태로 되돌리는 1단계 실행취소 기능 구현
