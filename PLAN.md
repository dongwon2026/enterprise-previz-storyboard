# PLAN.md

이번 PDCA 사이클(Plan)에서 PRD.md에 정의된 기능 전체를 구현하기 위한 작업 계획.

## 이번 사이클 목표

PRD.md에 정의된 두 가지 핵심 기능 — **① 예능 기획 아이디어 → 12프레임 텍스트 스토리보드 자동 생성**, **② 프레임 개별 수정/재생성** — 을 PIN 게이트, localStorage 저장, 내보내기(복사/.txt)까지 포함해 Next.js 위에서 완결된 형태로 구현하고 Vercel에 배포한다.

## 성공 기준 (PRD 3절 기준)

- 기획 아이디어 입력 후 12개 프레임 텍스트 스토리보드 생성까지 **1분 이내**
- 스토리보드 1건당 평균 **2건 이내**의 재생성 요청으로 원하는 결과에 도달
- PRD 5절의 규칙(12프레임 고정/서사 순서, 50~500자 입력, 프레임당 100자 이상, 2~10초 짝수 분량, 재생성 시 나머지 프레임 불변, 재생성 최대 10회, 1단계 되돌리기, 오류 시 자동 3회+수동 재시도)이 모두 코드로 지켜짐
- (계측) 생성/재생성에 걸린 시간과 프레임별 재생성 횟수를 로그로 남겨, 위 "1분 이내"/"평균 재생성 2건 이내" 기준 충족 여부를 실제로 확인할 수 있음
- 공유 비밀번호(PIN) 없이는 화면에 접근할 수 없고, 새로고침/재실행 후에도 마지막 작업 스토리보드가 유지됨
- Vercel에 배포되어 외부 URL로 접근 가능

## 작업 순서 (선행 작업 먼저)

1. Next.js 프로젝트 스캐폴딩 (package.json, App Router, 기본 레이아웃) 생성
2. 전역 스타일 뼈대 설정 (화이트 배경 + ENA Blue 포인트, KT Flow 서체, 최소 1440px PC 전용 레이아웃, 1440px 미만은 가로 스크롤만 허용) — 서체 파일은 `public/fonts/`에 준비, 미확보 시 시스템 기본 서체로 폴백
3. 공유 비밀번호(PIN) 게이트 구현 — `.env`의 `APP_ACCESS_PIN` 값과 서버에서 대조하는 `/api/auth` 라우트 + 성공 시 서명된 HttpOnly 쿠키 발급, 게이트 UI(미입력/오답 시 차단). 모든 요청 앞단에서 쿠키를 검사하는 코드는 Next.js 16 기준 파일명이 `proxy.ts`임(과거 버전의 `middleware.ts`와 같은 역할, 이름만 변경됨)
4. 기획 아이디어 입력 UI 구현 (50~500자 글자 수 카운터, 범위 밖이면 생성 버튼 비활성화)
5. OpenAI API 연동 유틸 작성 (`OPENAI_API_KEY` 로드, 경량형 모델·JSON 응답 모드 고정) + `zod`로 12프레임 응답 형식(12개/100자 이상/2~10초 짝수) 검증하는 공용 스키마 작성
6. 12프레임 생성 프롬프트 설계 (프레임 수 12개 고정, 서사 순서 배치·불일치 시 AI 자가 재배치, 입력 정보 부족 시 AI 자가 보완, 프레임당 100자 이상, 2~10초 짝수 분량 규칙 반영)
7. 스토리보드 생성 API 라우트 구현 (PIN 인증 쿠키 검사 → 입력 50~500자 서버 재검증 → OpenAI 호출 → zod 검증, 형식 위반 시 1회 재요청 → 12프레임 JSON 응답, Vercel 실행시간 제한 대비 `maxDuration` 설정)
8. 재사용 가능한 자동 재시도 유틸 작성 (1~2초 간격, 최대 3회) — 7번 생성 API에 우선 적용, 12번 재생성 API 완성 시 동일하게 적용 (zod 형식 재요청 1회와는 별개 로직)
9. 생성된 12프레임을 카드/리스트로 표시하는 UI 구현 (생성 중 화면 전체 로딩 표시, 이미 결과가 있는 상태에서 재생성 버튼 클릭 시 확인 후 전체 초기화)
10. 프레임 설명 텍스트 직접 편집 UI 구현 (클릭 시 타이핑 수정, 100자 미만으로 줄이면 저장 차단 — AI 호출 없이 클라이언트에서 즉시 처리)
11. 프레임별 분량(초) 수동 수정 UI 구현 (2/4/6/8/10초 중 선택)
12. 프레임 개별 재생성 UI + 전용 API 라우트 구현 (요청에 기획 아이디어 원문 + 선택 입력한 수정 지시 포함, 재생성해도 PD가 수동으로 바꾼 분량은 유지, 나머지 8개 프레임 내용·번호·순서는 변경하지 않음, 재생성 중 해당 카드만 로딩 표시)
13. 프레임별 재생성 요청 횟수 제한(최대 10회) 로직 구현
14. 자동 재시도 3회 모두 실패 시 에러 메시지 + 수동 "재시도" 버튼 UI 구현 (생성 실패는 상단 배너, 재생성 실패는 해당 카드 안에 인라인 표시)
15. 프레임 재생성 1단계 되돌리기(실행취소) 기능 구현 (재생성 직전 상태 임시 보관, 되돌려도 재생성 횟수는 그대로 유지, 최근 1단계만 지원)
16. `localStorage` 기반 상태 저장/복원 구현 — 기획 아이디어 원문 + 12프레임(설명/분량/재생성횟수) 저장, 새로고침·재실행 후에도 마지막 스토리보드 유지
17. 프레임별/전체 복사 버튼 구현
18. 텍스트 파일(.txt) 다운로드 기능 구현 (프레임 번호·분량·설명 순으로 나열, 파일명 `storyboard_YYYYMMDD-HHmm.txt`)
19. Vercel 배포 설정 및 환경변수(`OPENAI_API_KEY`, `APP_ACCESS_PIN`) 등록 후 배포

## 추가 기능 (PRD 최초 범위 밖, 사이클 진행 중 요청됨)

20. 캐릭터(인물) 설명 생성 기능 구현 — 예능 기획 아이디어 입력 옆에 독립 박스 3개(캐릭터 1/2/3, 서로 상태 공유 안 함)로 배치. 각 박스에서 인물 사진 1장(JPG/PNG/WEBP, 최대 4MB) 업로드 → "캐릭터 설명 생성" 클릭 → AI(비전 모델)가 외형을 상세한 한국어 문장으로 묘사(최소 80자) → 별도 결과 박스에 표시 + 복사 버튼. 사진은 저장하지 않고(localStorage·서버 모두 미저장) 설명 생성 1회 호출에만 사용
21. 인물 일관성 · 렌더 안정성 강화 구현 (DESIGN.md §6) — 프레임에 `castRefs`/`cameraNote`/`audioNote` 필드 추가, 전역 `styleLock`/`negativePrompt` 편집 영역 추가. 캐릭터1(진행자)/캐릭터2(고정 파트너) 설명이 채워진 채로 생성하면 프레임 전체에 자동 태깅되어 인물이 유지되고, 캐릭터 박스의 "이 인물을 프레임에 적용" 버튼으로 수동 태깅/해제, 프레임 카드의 태그 칩으로 개별 프레임에서 태그 해제 가능. 재생성 시 태깅된 인물 외형과 styleLock을 항상 고정 반영하고, 수정 지시는 동작·구도·소품에만 반영(외형 변경 지시는 서버가 무시, 화면엔 충돌 경고 표시). 동작 밀도 제한/읽는 글자 금지/촬영 스태프 노출 금지 규칙을 프롬프트에 반영. "전체 복사"/".txt 다운로드"는 STYLE LOCK → 태깅된 캐릭터 설명 → 프레임별(본문+카메라+오디오) → NEGATIVE PROMPT 순으로 조립
22. 프레임 수 9개 → 12개로 확장 — `storyboardSchema`(zod)/생성·재생성 프롬프트/프레임 번호 검증(1~12) 변경, 카드 그리드는 3열 자동 줄바꿈이라 12개면 자동으로 4행이 됨(레이아웃 코드 변경 불필요). CLAUDE.md/PRD.md/prd_lite.md/PLAN.md/DESIGN.md의 "9개/9프레임/1~9" 문구도 모두 12로 함께 갱신
23. 화면 한국어화 + 내보내기 영어 자동 번역 (DESIGN.md §7 최초 버전) — `lib/styleLockDefaults.ts`의 스타일 고정/네거티브 프롬프트 기본값을 한국어로 변경(PD 친화성). "전체 복사"/".txt 다운로드"는 구글 Flow Omni용으로 화면의 한국어 내용을 `POST /api/storyboard/export-translate`(신규, OpenAI 번역 + zod 구조 검증 + 자동 재시도 3회/형식 재요청 1회)로 번역한 뒤 영어 라벨로 재조립해서 내보낸다. 동일 내용을 다시 내보낼 때는 재번역 없이 직전 결과를 재사용(캐시) — 24번 작업에서 프레임 필드 구조와 내보내기 조립 방식이 크게 바뀌었다
24. 구글 Flow Omni 2-step(정지 이미지→영상) 워크플로우 최적화 포맷팅 (DESIGN.md §7 개정) — 프레임 필드를 `imageDescription`([IMAGE], 화각·배경·조명·표정·정지 포즈)/`videoAction`([VIDEO], 카메라 무빙 1개+단순 동작 1개)/`audioNote`([AUDIO])로 재구성(`description`+`cameraNote` 폐기). 8초 이상 프레임은 videoAction을 4초 단위 "비트"로 강제 분할(프롬프트 지시 + zod `.refine()` 구조 검증 + 형식 위반 시 재요청으로 실제 강제). imageDescription/videoAction에는 인물 옷차림·외모 묘사를 넣지 않도록 금지(캐릭터 설명으로만 전달). 내보내기 조립 시 모든 프레임의 [IMAGE] 끝에 그 프레임에 태깅된 캐릭터 영문 묘사 + styleLock 영문 전문을 코드가 무조건 자동 병합(Auto-Append)해서, 프레임 1개만 복사해도 캐릭터·스타일이 유지되는 자기완결형 블록이 되게 함 — "이 프레임 복사"도 이제 이 번역·조립을 거침(23번까지는 한국어 그대로였음). `lib/videoSafetyTags.ts`(신규) — 취식/빠른 액션/군중/화면 속 글자 4개 장르를 프레임의 한국어 원문에서 키워드로 감지해(AI 판단에 의존하지 않음) 해당 방어 문구를 [VIDEO] 끝에 코드가 결정적으로 추가

25. 기획 아이디어 구조화 입력(제목/장르/시청 타깃/프로그램 개요 표) + 레퍼런스 예능 반영 (DESIGN.md §8 신설) — 자유 서술형 한 덩어리였던 "예능 기획 아이디어" 입력을 표(4행)로 구조화. 제목/시청 타깃(짧은 텍스트, 필수)·장르(프리셋 18종 태그 다중 선택 + 기타 직접 입력, 1개 이상 필수)·프로그램 개요(자유 서술형, 기존 "50~500자" 규칙이 이 필드로 이동)로 나누고, `lib/storyboardIdea.ts`의 `composeIdeaText()`로 AI 프롬프트용 한 덩어리 텍스트로 합친다. 예능 기획 아이디어 옆에 레퍼런스 예능(이름+이미지) 입력 박스를 신설(`ReferenceShowInput.tsx`, `POST /api/reference`, `lib/referenceSchema.ts`/`referencePrompt.ts`) — 캐릭터 설명 생성과 같은 원칙으로 이미지 원본은 저장하지 않고, AI 비전 호출로 만든 "연출 톤앤매너 묘사 텍스트"만 저장해 스토리보드 생성/재생성 프롬프트(`lib/storyboardPrompt.ts`)에 참고 자료로 반영한다(스토리·소재·인물은 베끼지 않도록 프롬프트로 제어). 기존 localStorage에 저장된 예전 형식(`idea` 문자열)은 "프로그램 개요" 필드로 자동 이전해 복원한다.
26. 스타일 고정 자동 제안/네거티브 프롬프트 고정 + 캐릭터 4/5 추가 + 대사·자막 필드 + 레이아웃 4x3·가로폭 균일화 (DESIGN.md §9 신설) — ① 스타일 고정은 스토리보드 생성 시마다 AI가 이번 기획에 맞춰 새로 제안해 화면 값을 자동 갱신(`storyboardGenerationSchema`), 네거티브 프롬프트는 반대로 사용자가 준 고정 문구로 교체하고 더 이상 AI가 건드리지 않음. ② `CAST_CHARACTERS`를 캐릭터1~3 → 1~5로 확장(캐릭터4/5 role: 추가 출연자2/3). ③ 프레임에 `dialogue`(대사)/`caption`(자막) 필드 추가 — 화자가 한국인이면 한국어, 외국인이면 영어로 dialogue를 쓰고 caption은 항상 한국어 방송 자막(zod로 "대사 있으면 자막도 있어야 함" 짝 검증); [IMAGE]/[VIDEO] 시각 프롬프트에는 섞지 않고 내보내기 시 `[AUDIO]`와 `[NEGATIVE PROMPT]` 사이에 번역하지 않은 `[DIALOGUE]` 참고 블록으로만 붙임. ④ `ReferenceShowInput.tsx`/`CharacterDescriber.tsx` 제목을 "예능 기획 아이디어"와 같은 검은색 볼드로 통일(기존 ena-blue 제거). ⑤ 12프레임 그리드를 3열×4행 → 4열×3행으로, 캐릭터 행/아이디어+레퍼런스 행/프레임 그리드/내보내기 행을 모두 같은 `max-w-[1720px]`로 맞춰 전체 가로길이를 균일화.
27. 장르 태그 기반 4그룹 시각 매핑으로 스타일 고정(Style Lock) 생성 알고리즘 고도화 (DESIGN.md §9 "스타일 고정 생성 알고리즘 상세" 추가 — 26번의 "AI가 알아서 제안" 방식을 사용자가 준 구체적 규칙으로 대체) — `lib/storyboardPrompt.ts`에 `STYLE_LOCK_GENERATION_RULES` 신설: 장르 태그(프리셋 18종 + 기타 직접 입력)를 [감성/힐링]/[긴장/액션]/[스튜디오/화사함]/[방송 리얼리티] 4개 그룹의 카메라 렌즈·조명·색감·무빙 키워드로 매핑하고, 태그가 여러 그룹에 걸치면 자연스럽게 융합, 프리셋에 없는 직접 입력 태그는 내용 그대로가 아니라 시각적 무드로 해석해서 반영하도록 지시. 최초 생성 프롬프트에서는 기존 styleLock 초안을 더 이상 참고시키지 않음(장르 매핑 규칙과 충돌 방지, 프레임 재생성은 여전히 참고). 구글 Flow 붕괴 방지용 영문 필수 방어 문구("broadcast-safe exposure...")는 AI 번역에 맡기면 문구가 매번 달라짐을 실제 확인해, `lib/storyboardExport.ts`의 `STYLE_LOCK_MANDATORY_SUFFIX` 상수로 만들어 `buildFrameExportBlockEn`이 코드로 정확히 그대로(styleLock이 비어도 예외 없이) 덧붙이도록 변경.
28. 캐릭터/레퍼런스 예능 제목 위치·높이 조정 (DESIGN.md §9 "헤딩 색상·위치 통일" 보강) — "캐릭터 생성"/"레퍼런스 예능" 제목을 각 박스 내부에서 `StoryboardWorkspace.tsx`(부모)로 옮겨 "예능 기획 아이디어"처럼 박스 바깥 위쪽 왼쪽 정렬로 표시(캐릭터는 5개 박스를 대표하는 제목 하나, 박스 내부 제목은 "카릭터N"으로 축약). 레퍼런스 예능 박스와 예능 기획 아이디어 박스를 감싸는 행을 `items-start` → `items-stretch`로 바꾸고 레퍼런스 쪽을 `flex flex-col`+`flex-1`로 늘려 두 박스 높이를 맞춤.
29. 출연진 역할(캐릭터별) 입력 필드 추가 (DESIGN.md §9 "출연진 역할 입력" 신설) — 예능 기획 아이디어 표의 "시청 타깃"과 "프로그램 개요" 사이에 "출연진 역할" 행을 신설. 설명이 생성된 캐릭터만 골라 라벨(캐릭터N)+입력창을 보여주고(하나도 없으면 안내 문구), PD가 이번 기획에서 그 인물이 맡는 역할(예: "시골 이장님", 최대 40자)을 자유롭게 적을 수 있다. `characterRoles`(캐릭터id→역할 문자열) 상태를 신설해 localStorage에 함께 저장하고, `resolveCastMembers`에서 값이 있으면 `CAST_CHARACTERS`의 고정 role("진행자" 등)보다 우선해서 스토리보드 생성/재생성 프롬프트(`lib/storyboardPrompt.ts`)의 "고정 출연자 외형 설명" 블록에 반영한다(비어 있으면 기존 고정 role 유지). 서버(`app/api/storyboard(/frame)/route.ts`)는 이미 role을 임의 문자열로 받고 있어 스키마 변경은 불필요.
30. OpenAI 모델을 gpt-4o-mini로 고정 (`lib/openai.ts`) — 사용자 요청 "openAI API KEY를 활용하고 있다면 ChatGPT 모델을 GPT4o-mini로 고정해줘". 이 앱이 호출하는 3개 모델 상수(`STORYBOARD_MODEL`/`CHARACTER_MODEL`/`REFERENCE_MODEL`, 이전 값 `gpt-5.4-mini`)를 전부 `gpt-4o-mini`로 변경. JSON 응답 모드와 이미지 입력(비전) 둘 다 지원해 스토리보드/캐릭터 설명/레퍼런스 분위기 묘사 3용도 모두에 그대로 쓸 수 있음 — `.env`의 `OPENAI_API_KEY`로 실제 API 호출까지 성공하는 것을 확인(응답 모델: `gpt-4o-mini-2024-07-18`).
31. 발화 묘사 제거 및 비언어적 표현 자동 치환 (DESIGN.md §10 신설) — 사용자 요청 "구글 Flow(Omni) 같은 영상 AI 모델은 발화(립싱크)를 묘사하거나 화면에 자막을 렌더링하도록 지시하면 얼굴 픽셀이 기괴하게 뭉개지는 치명적인 에러가 발생한다". 2단계 방어: ① `lib/storyboardPrompt.ts`의 `RENDER_SAFETY_RULES`(한국어 생성)에 발화 행위 묘사 금지 + 표정·제스처 치환 예시, 비언어적 소리 어휘, 화면 자막 요청 무시 규칙 추가. ② `lib/exportTranslatePrompt.ts`(영어 번역, 실제 Flow Omni로 나가는 마지막 방어선)에 talk/speak/say/dialogue/conversation/chat 등 발화 동사를 절대 쓰지 않고 100% 표정·제스처로 "재구성"하도록 지시(사용자가 준 치환 예시 3개 포함), audioNote는 비언어적 소리로만, 화면 자막 요청은 생략. ③ `lib/exportTranslateSchema.ts`에 `superRefine`으로 imageDescription/videoAction에 발화 동사가 남아 있으면 형식 위반 처리해 기존 1회 재요청 루프를 재사용(코드 검증 — 프롬프트만으로는 예외 없음이 보장되지 않아 추가). ④ `lib/storyboardExport.ts`에 `AUDIO_NO_DIALOGUE_SUFFIX`("No intelligible dialogue.") 상수를 신설해 `STYLE_LOCK_MANDATORY_SUFFIX`와 같은 방식으로 모든 [AUDIO] 줄 끝에 코드가 무조건 부착. ⑤ `[DIALOGUE]` 블록을 `buildFrameExportBlockEn`에서 100% 제거 — 내보내기 최종 블록은 [IMAGE]/[VIDEO]/[AUDIO]/[NEGATIVE PROMPT] 4개로 고정. dialogue/caption 필드 자체(생성·저장·화면 표시)는 유지하되 "참고용, 실제 영상에는 반영되지 않음" 안내를 `FrameCard.tsx`에 추가.
32. AI 형식 재요청 신뢰성 강화 (DESIGN.md §11 신설) — 사용자 요청 "AI 응답이 12프레임 형식 규칙을 지키지 않았습니다가 가끔 나오는데 무조건 12프레임 나오도록 강제해줘". `/api/storyboard`·`/api/storyboard/frame`·`/api/storyboard/export-translate` 3개 라우트가 각자 갖고 있던 재시도 루프를 `lib/aiJsonRetry.ts`(`requestJsonWithRetry`)로 통합. 디버그 로그로 실측한 진짜 실패 원인(프레임 개수가 아니라 "1~2개 프레임의 imageDescription 100자 미달"·"비트 분할 형식 누락" 같은 부분 위반)에 맞춰, 형식 위반 시 같은 프롬프트를 맹목적으로 반복하던 기존 방식을 "실패 응답 + zod가 잡아낸 구체적 문제 목록을 대화 기록에 남겨 그 부분만 고쳐서 다시 보내라"는 피드백 기반 재요청으로 바꿈(실측 — 재요청 1회로 문제 8개→1개로 감소 확인). `max_tokens`를 12000으로 넉넉히 고정해 응답이 잘려 JSON이 깨지는 경우도 방지. 프롬프트(`lib/storyboardPrompt.ts`)의 "정확히 12개"/"100자 이상" 규칙에도 강조 문구 + 자기 검토 지시 추가. 재시도 3회로 실측했을 때 Vercel `maxDuration`(60초)을 넘기는 사례(로컬 100초 이상)를 확인해 사용자에게 "재시도 확대(Vercel Pro 필요)" vs "현재 상태 유지" 중 선택을 물었고 "현재 상태 유지"로 결정 — `requestJsonWithRetry` 기본 `maxAttempts`는 2회 유지, 응답이 가벼운 프레임 재생성만 개별적으로 3회로 상향.

## 참고 문서

- [PRD.md](PRD.md) — 전체 기획서
- [prd_lite.md](prd_lite.md) — 요약본
- [CLAUDE.md](CLAUDE.md) — 작업 규칙 및 핵심 비즈니스 규칙
