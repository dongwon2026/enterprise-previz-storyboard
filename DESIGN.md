# DESIGN.md

[PLAN.md](PLAN.md)의 작업 순서를 실제로 구현하기 위한 설계 문서.

## 1. 화면 구성 (무엇이 어디에 보이는가)

전체 1페이지 구성(라우팅 없음), PC 전용(최소 1440px), 화이트 배경 + ENA Blue(`#2c24ce`) 포인트.

```
┌─────────────────────────────────────────────┐
│  [PIN 게이트] 최초 진입 시 전체 화면을 덮음        │
│  - 비밀번호 입력창 + 확인 버튼                    │
│  - 틀리면 "비밀번호가 올바르지 않습니다" 표시        │
│  - 맞으면 아래 메인 화면으로 전환                  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  [상단1] 캐릭터1~5 설명 생성 (5칸 균등 그리드, §5·§9 참고) │
│  - 각 칸 제목은 "예능 기획 아이디어"와 같은 검은색 볼드 글씨│
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  [상단2] 기획 아이디어 입력 영역 (표 형식) + 레퍼런스 예능 박스 │
│  - 표(4행): 제목 / 장르(프리셋 태그 다중선택+기타 직접입력) /  │
│    시청 타깃 / 프로그램 개요                        │
│  - 프로그램 개요만 글자 수 카운터 (예: "128 / 500 (최소 50자)")│
│    · 500자를 넘는 입력은 아예 막고, 50자 미만이면 버튼 비활성화 + 안내 문구│
│  - 옆에 레퍼런스 예능 박스: 이름 입력 + 이미지 업로드 +      │
│    "레퍼런스 분위기 분석" 버튼 (§8 참고), 제목도 검은색 볼드│
│  - "스토리보드 생성" 버튼 (제목/장르/시청 타깃도 채워야 활성)  │
│    · 이미 생성된 결과가 있으면 클릭 시 "새로 만들면 기존 내용이 사라집니다" 확인 후 진행│
│    · 생성 중에는 화면 전체에 로딩 표시, 버튼 중복 클릭 방지│
│  - 오류 발생 시: 에러 배너 + "재시도" 버튼(수동)      │
│  - 스타일 고정/네거티브 프롬프트 편집 영역 (§9 참고 — 스타일 고정만 생성 시마다 자동 갱신)│
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  [중단] 12프레임 카드 (4열 × 3행 그리드, 1~12번 순서, §9)│
│  각 카드 안에는:                                 │
│  - 프레임 번호 (1~12, 고정)                       │
│  - 프레임 설명 텍스트 (100자 이상)                  │
│    · 클릭하면 직접 타이핑해서 수정 가능 (100자 미만으로 줄이면 저장 차단 + 안내)│
│  - 분량(초) 선택 (2/4/6/8/10초 중 하나, 직접 변경 가능)│
│  - 카메라·동작/오디오 표시줄 + 대사·자막 표시줄(있을 때만, §9)│
│  - 수정 지시 입력창 (선택, 비워도 됨) — 예: "더 밝게", "야외로"│
│  - "재생성" 버튼 + 사용 횟수 표시 (예: "재생성 3/10회 사용")│
│    · 재생성 중에는 해당 카드만 로딩 표시              │
│    · 재생성 실패 시 카드 안에 에러 문구 + "재시도" 버튼 인라인 표시│
│  - "방금 되돌리기" 버튼 (직전에 재생성한 카드만 활성, 되돌려도 사용 횟수는 그대로 유지)│
│  - "이 프레임 복사" 버튼                          │
└─────────────────────────────────────────────┘

캐릭터 행/아이디어+레퍼런스 행/12프레임 그리드/내보내기 영역 모두 같은 max-w-[1720px] 컨테이너
안에서 가로 폭을 꽉 채우도록 맞춰(그리드·flex-1 활용) 전체 가로길이가 균일하게 보이도록 한다 (§9).

┌─────────────────────────────────────────────┐
│  [하단] 내보내기 영역                             │
│  - "전체 복사" 버튼                              │
│  - "텍스트 파일(.txt) 다운로드" 버튼                │
│    · 파일명: storyboard_YYYYMMDD-HHmm.txt        │
│    · 내용: 프레임 번호 + 분량(초) + 설명을 순서대로 나열│
└─────────────────────────────────────────────┘
```

화면 폭이 1440px 미만이면 레이아웃은 그대로 두고 가로 스크롤만 허용한다 (별도 반응형 대응은 하지 않음, PRD의 "PC 전용" 범위 유지).

## 2. 데이터 흐름 (입력 → 처리 → 출력)

**① 최초 생성**
```
PD가 기획 아이디어 입력 (제목/장르/시청 타깃/프로그램 개요 표 + 선택 입력 레퍼런스 예능)
  → (화면) 제목/시청 타깃 비어있지 않은지, 장르 1개 이상 선택했는지, 프로그램 개요 50~500자
    범위 확인, 하나라도 어긋나면 "스토리보드 생성" 버튼 비활성화
  → "스토리보드 생성" 클릭 (이미 결과가 있으면 확인 팝업 후 진행), 화면 전체 로딩 표시
  → 브라우저가 서버(API 라우트)에 4개 구조화 필드 + 레퍼런스(선택) 전송
    브라우저→서버 전송 자체가 실패(네트워크 단절 등)하면 자동 재시도 없이
    에러 배너 + 수동 "재시도" 버튼만 노출 (아래 OpenAI 구간 자동 재시도와는 다른 계층)
  → 서버도 4개 필드 각각의 규칙(제목/시청 타깃 비어있지 않음, 장르 1개 이상, 개요 50~500자)을
    다시 확인 (화면 검증을 우회해 API를 직접 호출하는 경우 방지)
    범위를 벗어나면 `VALIDATION_ERROR`로 즉시 거절, OpenAI 호출 자체를 하지 않음
  → 서버가 `lib/storyboardIdea.ts`의 `composeIdeaText`로 4개 필드를 한 덩어리 텍스트로 합친 뒤,
    OpenAI에 "12프레임 규칙"이 담긴 프롬프트로 요청 (레퍼런스가 있으면 톤앤매너 참고 블록도 함께 포함)
    (OpenAI 응답 실패 시 서버가 1~2초 간격, 최대 3회 자동 재시도 — 서버↔OpenAI 구간에서만 발생)
  → OpenAI가 12개 프레임(설명 + 추천 분량)을 응답
  → 서버가 zod로 형식(12개 / 각 100자 이상 / 2~10초 짝수 / dialogue·caption 짝 맞음)을 검증
    형식이 어긋나면 동일 프롬프트로 1회 더 요청 → 그래도 어긋나면 `AI_FORMAT_ERROR` 응답
    (이 1회 재요청은 "형식 보정"용으로, 위 자동 재시도 3회와는 별개)
  → AI는 12프레임과 함께 이번 기획에 맞는 styleLock 문구도 top-level로 새로 제안한다
    (사용자 요청 — "스타일 고정을 스토리보드에 맞게 그때그때 수정", §9 참고)
  → 브라우저로 전달 후 화면에 12프레임 카드로 표시, styleLock 값을 자동으로 화면에 반영, 로딩 해제
  → 기획 아이디어 원문 + 12프레임 + styleLock을 함께 브라우저 저장소(localStorage)에 즉시 보관
    (원문을 같이 저장해야 이후 프레임 재생성 시 기획 의도를 다시 사용할 수 있음)
```

**② 프레임 개별 재생성 / 직접 편집**
```
PD가 특정 프레임의 "재생성" 클릭 (해당 프레임 횟수 10회 미만일 때만 가능)
  → 재생성 직전 상태(설명·분량)를 임시로 1개만 보관 (되돌리기용)
  → 해당 카드만 로딩 표시
  → 브라우저가 서버에 "기획 아이디어 원문 + 몇 번 프레임 + 나머지 8개 내용 + (선택) 수정 지시"를 함께 전송
    (원문 없이는 AI가 처음 의도를 모른 채 재생성하게 되어 품질이 떨어짐)
    전송 자체가 실패하면 자동 재시도 없이 카드 안에 에러 + 수동 "재시도" 버튼 노출
  → 서버가 OpenAI에 "이 프레임만 다시 써줘" 요청 (①과 동일하게 자동 재시도 3회 + zod 검증 1회 재요청)
  → 응답받은 새 설명만 화면에서 교체 (나머지 8개는 그대로)
    분량(초)은 PD가 이미 수동으로 바꿔둔 값이 있으면 그대로 유지하고, AI가 새로 제안한 분량으로 덮어쓰지 않는다
  → 재생성 횟수 +1, localStorage 갱신
PD가 "되돌리기" 클릭 시
  → 임시 보관해둔 직전 상태(설명·분량)로 그 프레임만 되돌리고, 임시 보관은 비움
  → 재생성 횟수는 되돌리지 않고 그대로 유지 (최대 10회 제한을 우회하지 못하도록)
PD가 프레임 설명을 직접 타이핑해서 수정할 때
  → 100자 이상이면 그대로 저장, 100자 미만으로 줄이면 저장을 막고 안내 문구 표시
  → AI 호출 없이 클라이언트에서 바로 처리되어 localStorage에 반영
```

**③ 저장/복원**
```
프레임 내용·분량·재생성 횟수 등 상태가 바뀔 때마다
  → localStorage에 자동 저장 (저장 항목: 기획 아이디어 원문, 12프레임 각각의 설명/분량/재생성횟수)
새로고침 또는 브라우저 재실행
  → localStorage에서 마지막 상태를 읽어 화면 복원
```

**④ 내보내기**
```
"이 프레임 복사" 클릭 → 그 프레임 설명 문장(한국어) 그대로 클립보드로 복사
"전체 복사" 클릭 → 화면의 한국어 내용(STYLE LOCK/캐릭터/프레임/NEGATIVE PROMPT)을
  POST /api/storyboard/export-translate로 보내 영어로 번역 → 번역 결과를 클립보드로 복사
  (사용자 요청 §7 — 구글 Flow Omni는 영어 프롬프트를 전제로 하므로 내보낼 때만 번역한다)
".txt 다운로드" 클릭 → 위와 동일하게 영어로 번역한 뒤
  `storyboard_YYYYMMDD-HHmm.txt` 파일명으로 다운로드
```

**⑤ PIN 게이트**
```
PD가 비밀번호 입력 → 브라우저가 서버(API 라우트)로 값 전송
  → 서버가 .env의 APP_ACCESS_PIN 값과 비교 (비밀번호 자체는 브라우저 코드에 노출되지 않음)
  → 맞으면 서버가 서명된 HttpOnly 쿠키를 발급 (유효기간 12시간, 브라우저 스크립트로는 읽거나 위조 불가)
  → 틀리거나 안 넣으면 계속 게이트 화면에 머무름
그 이후 모든 요청 (스토리보드 생성/재생성 API 포함)
  → 서버가 이 쿠키를 매번 검사, 없거나 만료되면 401(UNAUTHORIZED)로 거절
    (PIN 화면만 가리고 API는 그대로 열려 있으면, 주소를 아는 사람이 인증 없이
     OpenAI 호출을 유발할 수 있어 반드시 서버 쪽에서도 막아야 함)
```

## 3. API 계약 (화면 코드와 서버 코드가 지켜야 할 약속)

| 엔드포인트 | 요청 | 성공 응답 | 실패 응답 |
|---|---|---|---|
| `POST /api/auth` | `{ pin }` | `{ data: { ok: true } }` + HttpOnly 쿠키 발급 | `{ error: { code: "INVALID_PIN", message } }` |
| `POST /api/storyboard` | `{ title, genres: string[], target, overview, reference?: {showName?, description?}, styleLock?, castMembers?: [{id,label,role,description}] }` | `{ data: { idea, frames: [{ no, imageDescription, durationSec, videoAction, audioNote, dialogue, caption }], styleLock } }` (`idea`는 서버가 4개 필드를 합쳐 조립한 텍스트, `styleLock`은 이번 기획에 맞춰 AI가 새로 제안한 값 — §9) | `{ error: { code, message } }` |
| `POST /api/storyboard/frame` | `{ title, genres, target, overview, reference?, frameNo, frames, instruction?, styleLock?, castMembers?: [{id,label,role,description}] }` (최초 생성 때와 동일한 구조화 필드를 그대로 다시 보내 서버가 같은 문구로 재조립, 재생성 대상 프레임 번호 + 나머지 8개 + 선택 입력한 수정 지시 + 이 프레임에 태깅된 캐릭터 설명 포함) | `{ data: { frame: { no, imageDescription, durationSec, videoAction, audioNote, dialogue, caption } } }` | `{ error: { code, message } }` |
| `POST /api/character` | `{ imageDataUrl }` (JPG/PNG/WEBP data URL, 원본 최대 4MB) | `{ data: { description } }` | `{ error: { code, message } }` |
| `POST /api/reference` | `{ imageDataUrl }` (JPG/PNG/WEBP data URL, 원본 최대 4MB) | `{ data: { description } }` (레퍼런스 이미지의 연출 톤앤매너 묘사) | `{ error: { code, message } }` |
| `POST /api/storyboard/export-translate` | `{ styleLock, negativePrompt, castDescriptions: [{id,label,role,description}], frames: [{no,imageDescription,videoAction,audioNote}] }` (durationSec은 번역 대상이 아니라 보내지 않음, 화면이 no로 다시 짝지음) | `{ data: { styleLock, negativePrompt, castDescriptions, frames } }` (같은 구조, 값만 영어로 번역됨. id는 캐릭터를 프레임별로 다시 매칭하기 위한 값이라 번역하지 않고 그대로 돌아온다) | `{ error: { code, message } }` |

- `/api/auth`를 제외한 모든 API는 PIN 인증 쿠키가 없으면 항상 `401 UNAUTHORIZED`
- 에러 코드는 다음 4가지로 고정: `VALIDATION_ERROR`(제목/시청 타깃 미입력, 장르 미선택, 개요 50~500자 등 입력 규칙 위반), `UNAUTHORIZED`(인증 없음/만료), `AI_FORMAT_ERROR`(AI 응답이 12프레임/imageDescription 100자/짝수초/8초 이상 비트 분할 규칙에 안 맞음, 1회 재요청 후에도 실패 — export-translate/reference는 같은 코드를 각자의 결과 형식 불일치에도 사용), `UPSTREAM_ERROR`(OpenAI 자체 오류, 자동 재시도 3회 모두 실패)
- 프레임 설명을 PD가 직접 타이핑해서 편집하는 것은 AI를 호출하지 않는 순수 클라이언트 동작이라 별도 API가 없음 — 100자 이상 규칙만 클라이언트에서 확인 후 localStorage에 바로 반영

## 4. 기술 선택

**기본(고정, PRD 8절):**
- **Next.js** — 화면(사용자가 보는 부분)과 API(서버가 처리하는 부분)를 한 프로젝트 안에서 같이 만들 수 있는 도구. 마지막 날 **Vercel**에 그대로 올려서 배포한다.

**추가로 필요한 기술 (필수):**
| 기술 | 쉬운 설명 |
|---|---|
| `openai` (OpenAI 공식 라이브러리) | "AI에게 스토리보드 문장을 만들어달라고 요청"하는 심부름을 대신 해주는 공식 도구. 이게 있어야 서버에서 OpenAI에 문의할 수 있다. 모델은 비용 효율적인 경량 모델(JSON 응답 모드 지원)을 기본값으로 고정하고, 결과 품질이 부족하면 이후에 더 상위 모델로 교체한다. |
| `zod` (형식 검증 도구) | AI가 만들어 준 결과가 "12개, 각 100자 이상, 짝수 초" 같은 PRD 필수 규칙을 실제로 지켰는지 서버 코드가 자동으로 검사하게 해주는 도구. 규칙을 어긴 결과가 그대로 화면에 나가는 걸 막아주므로, 이 프로젝트에서는 선택이 아니라 필수로 둔다. (검증 실패 시 동작은 위 "2. 데이터 흐름 ①" 참고) |
| Next.js 내장 폰트 기능 | KT Flow 서체 파일을 웹페이지에 깨지지 않고 예쁘게 불러와 보여주는 Next.js 기본 기능. 별도 설치가 필요 없다. 단, 폰트 파일 자체(`.woff`/`.woff2` 등)는 사내에서 보유한 라이선스 파일을 `public/fonts/`에 직접 넣어줘야 하며, 파일이 없을 경우를 대비해 시스템 기본 서체로 자동 대체(폴백)되게 만든다. |
| Tailwind CSS (스타일링 도구) | 흰 배경 + ENA Blue 포인트 컬러처럼 단순한 디자인을 빠르고 일관되게 입힐 수 있게 도와주는 도구. 스타일 코드가 깔끔해지고 사고 위험이 줄어들어 채택하기로 확정한다. |

**배포 관련 추가 설정:**
- Vercel은 API 요청 하나가 실행될 수 있는 시간에 제한이 있다. 12프레임을 한 번에 만드는 요청은 시간이 걸릴 수 있으므로, 스토리보드 생성/재생성 API 라우트에 "이 요청은 최대 60초까지 기다려도 된다"는 설정(`maxDuration`)을 넣어 중간에 끊기지 않게 한다.

**의도적으로 추가하지 않는 것:**
- 별도 데이터베이스, 로그인/회원가입 라이브러리 — PRD 비범위이며 PIN 게이트 + localStorage만으로 충분
- 별도 상태관리 라이브러리(Redux 등) — 화면이 1페이지라 React 기본 기능만으로 충분

## 5. 캐릭터(인물) 설명 생성 기능 (PRD 최초 범위 밖, 사이클 진행 중 추가됨 — PLAN.md 20번)

기획 아이디어 입력 영역 오른쪽에 독립 박스로 배치. 스토리보드 생성/재생성과는 별개로 동작하며, 결과가 자동으로 다른 곳에 쓰이지 않는다(PD가 직접 복사해서 사용).

```
[예능 기획 아이디어 입력]     [캐릭터 설명 생성]
(기존과 동일)                - 사진 선택 (JPG/PNG/WEBP, 최대 4MB)
                              - 선택한 사진 미리보기
                              - "캐릭터 설명 생성" 버튼
                              - 생성 중 이 박스 안에서만 로딩 표시
                              - 결과: 별도 박스에 인물 묘사 텍스트 + "복사" 버튼
                              - 실패 시 에러 문구 + "재시도" 버튼(인라인)
```

**데이터 흐름:** 사진 선택(브라우저 메모리에만 존재, 미리보기용 objectURL) → "캐릭터 설명 생성" 클릭 → 사진을 base64로 인코딩해 `POST /api/character`로 전송 → 서버가 확장자·용량(4MB) 재검증 → OpenAI 비전 모델에 인물 묘사 요청(자동 재시도 3회 + 형식 위반 시 1회 재요청, `/api/storyboard`와 동일한 2계층 구조) → zod로 결과 형식(최소 80자) 검증 → 결과 텍스트만 화면에 표시. **사진 원본은 어디에도 저장하지 않는다** — localStorage에도 안 담기고, 서버에도 남기지 않으며, 이번 API 호출 1회에만 사용되고 사라진다(개인 사진이라 최소한으로만 다룬다).

**독립된 슬롯 5개를 균등한 폭으로 나란히 둔다** (캐릭터 1/2/3/4/5, 사용자 요청으로 3개 → 5개 확장, §9 참고). 슬롯마다 사진 1장 → 설명 1개만 처리하고, 슬롯끼리 상태를 전혀 공유하지 않는다. 6번째 인물부터는 슬롯 하나를 새 사진으로 교체해 다시 생성한다. 각 슬롯 제목은 "예능 기획 아이디어"와 통일된 검은색 볼드 글씨로 표시한다(기존에는 ENA Blue였음, §9).

## 6. 인물 일관성 · 렌더 안정성 강화 (PRD 최초 범위 밖, 사이클 진행 중 추가됨)

목표: 이 앱이 만든 결과물을 그대로 구글 Flow Omni에 붙여넣었을 때 인물이 일관되게 유지되고, 렌더 실패 요소(읽는 글자·촬영 스태프 노출·립싱크 실패)가 없고, 초 단위 분량에 맞는 동작 밀도를 갖게 한다.

**데이터 모델 확장**
- 프레임에 `castRefs: string[]`(태깅된 캐릭터 id 목록) 필드 추가. (`imageDescription`/`videoAction`/`audioNote` 3필드 분리는 §7에서 더 크게 다시 다룬다.)
- 전역 상태에 `styleLock`(기본값: 영상 스타일 톤 템플릿, `lib/styleLockDefaults.ts`)과 `negativePrompt`(기본값: 렌더 방지 요소 템플릿) 추가. PD가 아이디어 입력 아래 텍스트 영역에서 직접 편집 가능하며 localStorage에 함께 저장된다. 화면 기본값은 한국어(PD 친화성, 사용자 요청 §7)이며, 영어 버전은 저장하지 않고 내보내기 시점에 AI가 번역해서 만든다.

**캐릭터1(진행자)/캐릭터2(고정 파트너) 기본 인물 일관성**
- 두 캐릭터 패널이 채워진 상태로 스토리보드를 생성하면, 생성된 12프레임 전부에 자동으로 두 캐릭터가 태깅(castRefs)되고, 프롬프트에도 두 인물의 외형 설명이 "고정 출연자"로 전달되어 AI가 12프레임 내내 같은 인물로 유지하도록 지시받는다. 캐릭터3/4/5(추가 출연자)는 자동 태깅하지 않고 PD가 수동으로 태깅한다.
- 캐릭터 설명 결과 박스의 "이 인물을 프레임에 적용" 버튼을 누르면 그 캐릭터를 12프레임 전체에 토글 태깅한다. 프레임 카드에 표시되는 태그 칩을 클릭하면 그 프레임 1개에서만 태그를 뺄 수 있다 (예: 1번 프레임은 캐릭터1만, 2번 프레임은 캐릭터1+캐릭터2).
- 두 캐릭터 패널이 모두 비어 있는 상태로 생성하면, 생성 직후 "진행자/파트너 캐릭터를 먼저 만들면 프레임 인물이 훨씬 안정적으로 유지됩니다"라는 안내 배너를 보여준다 (닫기 가능).

**재생성 시 인물 고정**
- 프레임을 재생성할 때, 그 프레임에 태깅된 캐릭터 설명 전문과 styleLock을 항상 함께 서버로 보내 AI가 인물 외형을 유지하도록 지시한다. PD의 수정 지시는 동작·구도·소품에만 반영되고, 지시 내용이 태깅된 인물의 외형(성별·나이·체형 등)을 바꾸려는 것이면 그 부분은 무시하도록 서버 프롬프트에 명시한다.
- 화면에서는 태깅된 프레임에 외형 변경으로 보이는 키워드(예: "여자로 바꿔줘")가 수정 지시에 포함되면 "인물 일관성과 충돌할 수 있다"는 경고 문구를 보여준다 (차단은 아니고 안내만, 실제 방지는 서버 프롬프트가 담당).

**렌더 안정성 프롬프트 규칙** (모두 `lib/storyboardPrompt.ts` 시스템 프롬프트로 강제 — 대부분 프롬프트 지시로만 보장하지만, 8초 이상 비트 분할만은 zod로도 구조 검증한다. §7 참고)
- 읽는 글자가 나오는 요소(간판·포스터·손글씨 메뉴판·스마트폰/태블릿 화면·지도 앱·로고·소품 글자) 금지. 필요 시 "아웃포커스 처리되어 읽을 수 없음"을 문장에 명시.
- 촬영 스태프(제작진·카메라 크루·마이크 붐) 프레임 내 등장 금지.
- audioNote는 앰비언스·SFX만, 대사 절대 금지(립싱크 실패 방지). 대사 자체는 §9의 별도 dialogue/caption 필드로만 다루며, [IMAGE]/[VIDEO] 시각 프롬프트에는 절대 섞이지 않는다.

**주의 (기존 저장 데이터 호환성)**: `imageDescription`/`videoAction`/`audioNote`/`castRefs`/`dialogue`/`caption`이 없는 예전 형식으로 저장된 localStorage 스토리보드는 새 스키마와 맞지 않아 자동으로 무효 처리되어 사라진다. 새로고침 후 스토리보드를 다시 생성해야 한다.

## 7. 구글 Flow Omni 2-step 워크플로우 최적화 포맷팅 + 영어 자동 번역 (PRD 최초 범위 밖, 사이클 진행 중 추가·개정됨)

목표: 화면(스타일 고정/네거티브 프롬프트/프레임 필드)은 PD가 읽고 편집하기 편하도록 한국어로 유지하면서, 실제로 구글 Flow Omni(영어 기반, "① 정지 이미지 생성 → ② 그 이미지를 시작 프레임으로 영상 생성" 2-step)에 붙여넣는 내보내기 결과물만 이 2-step 구조에 맞춘 영어 텍스트로 자동 조립·번역한다.

**프레임 필드 3분리 — `[IMAGE]`/`[VIDEO]`/`[AUDIO]`**
- `imageDescription` → `[IMAGE]`: "정지된 사진 한 장"을 만드는 프롬프트. 화각(와이드샷/클로즈업 등)·배경·조명·인물의 표정과 정지된 포즈만 담는다. 동작·카메라 무빙은 절대 섞지 않는다. 최소 100자 이상(zod 검증).
- `videoAction` → `[VIDEO - Ns]`: 그 정지 이미지에서 짧은 영상을 만드는 프롬프트. "카메라 무빙 1개 + 아주 단순한 동작 1개"만 담는다. `durationSec`이 8초 이상이면 4초를 넘지 않는 단위로 "비트 1 (0~4초): .../비트 2 (4~8초): ..." 형식으로 강제 분할한다 — 이 형식은 프롬프트 지시뿐 아니라 `lib/storyboardSchema.ts`의 zod `.refine()`으로도 검증해서, AI가 지키지 않으면 기존 "형식 위반 시 1회 재요청" 루프가 자동으로 재시도한다.
- `audioNote` → `[AUDIO]`: 앰비언스·SFX만, 대사 절대 금지(변경 없음).
- imageDescription/videoAction 어디에도 인물의 옷차림·외모 묘사를 넣지 않는다 — 프레임마다 옷이 바뀌면 AI가 다른 사람으로 인식하기 때문에, 외형은 오직 캐릭터 설명(고정 출연자)으로만 전달한다.

**CAST & STYLE 자동 병합 (Auto-Append) — 인물 일관성의 핵심 뼈대**
- 내보내기 데이터를 조립할 때, 모든 프레임의 `[IMAGE]` 맨 마지막에 "그 프레임에 태깅된(castRefs) 캐릭터의 영문 외형 묘사"와 "스타일 고정(styleLock) 영문 전문"을 코드가 무조건 자동으로 덧붙인다. 그 결과 프레임 1개짜리 블록(`[IMAGE]`+`[VIDEO]`+`[AUDIO]`+대사가 있으면 `[DIALOGUE]`(§9)+`[NEGATIVE PROMPT]`)이 항상 "자기 완결형"이 되어, PD가 씬 하나만 골라 복사해 구글 Flow에 붙여넣어도 룩앤필과 캐릭터가 그대로 유지된다. (예전처럼 STYLE LOCK/캐릭터 설명을 문서 맨 위에 한 번만 두는 방식은 폐기했다 — 12프레임 전체를 내보낼 때도 프레임마다 반복해서 붙는다.)

**장르별 방어 태그 자동 삽입 (Auto-Safe Tags)** — `lib/videoSafetyTags.ts`
- "예외 없이 완벽하게 작동"해야 하는 규칙이라 AI 판단에 맡기지 않고, 코드가 프레임의 한국어 원문(imageDescription+videoAction)에서 키워드를 직접 검사해 결정한다. 감지되면 번역된 `[VIDEO]` 프롬프트 맨 끝에 해당 영어 방어 문장을 그대로 덧붙인다.
- 취식/먹방(먹다·마시다·베어 물다·삼키다·들이켜다 등) → "Keep hands in natural motion blur. Do not hold on an open chewing mouth for more than one second."
- 달리기/격투/서바이벌/춤 등 빠른 액션(뛰다·달리다·싸우다·넘어지다·춤 등) → "Keep rapid limb movements in natural motion blur. Avoid extreme body contortions."
- 여행/관찰 등 군중 장면(군중·인파·붐비다·행인 등) → "Keep background crowd out of focus. Maintain clear focus only on main characters."
- 미션 카드/스마트폰/간판 등 등장(미션 카드·스마트폰·간판·포스터 등) → "Keep all text, UI overlays, and signage entirely blurred and non-legible."

**동작 방식 (`POST /api/storyboard/export-translate`)**
- "전체 복사"/".txt 다운로드"/"이 프레임 복사" 버튼을 누르면, 화면에 있는 한국어 내용(styleLock, negativePrompt, 태깅된 캐릭터 설명 — id 포함, 12프레임의 imageDescription/videoAction/audioNote)을 이 API로 보낸다.
- 서버가 OpenAI(경량 모델, JSON 응답 모드)로 번역을 요청하고, `lib/exportTranslateSchema.ts`(zod)로 응답 구조가 그대로 유지됐는지 검증한다 (형식 위반 시 1회 재요청, 네트워크 오류 시 자동 재시도 3회 — `/api/storyboard`와 동일한 2계층 구조 재사용). 비트 분할("Beat 1 (0-4s): ...")도 구조를 유지한 채 번역하도록 지시한다.
- 화면(`StoryboardWorkspace.tsx`)이 번역 결과를 받아 `lib/storyboardExport.ts`의 `buildFrameExportBlockEn`(프레임 1개)/`buildStoryboardExportTextEn`(전체)으로 위 CAST&STYLE 자동 병합 + 장르별 방어 태그를 적용해 최종 텍스트를 조립한다. 분량(초)·castRefs는 번역 대상이 아니라 화면에 있는 원본 값을 프레임 번호(no)로 다시 짝지어 그대로 사용한다.
- 번역 중에는 관련 버튼을 비활성화하고 "영어로 번역 중..."을 표시한다. 실패하면 에러 문구를 보여주고, 버튼을 다시 누르면 재시도된다.
- 직전 번역 이후 화면 내용이 하나도 바뀌지 않았으면(같은 입력) 다시 AI를 호출하지 않고 방금 번역한 결과(원본 구조 그대로의 `ExportTranslateResult`)를 그대로 재사용한다 — "전체 복사"→".txt 다운로드"→"이 프레임 복사"를 연달아 눌러도 번역 API는 한 번만 호출된다.
- "이 프레임 복사"도 이제 이 번역·조립 과정을 거친다 (예전에는 한국어 원문만 복사했지만, "씬 하나만 복사해도 완결되게" 하려면 캐릭터·스타일 자동 병합이 필요하기 때문).

## 8. 기획 아이디어 구조화 입력 + 레퍼런스 예능 (PRD 최초 범위 밖, 사이클 진행 중 추가됨 — PLAN.md 25번)

목표: 자유 서술형 텍스트 한 덩어리였던 "예능 기획 아이디어" 입력을 PD가 실제로 기획안을 쓰는 방식(제목/장르/시청 타깃/개요)에 맞춰 표 형식으로 구조화하고, 참고하고 싶은 기존 예능의 톤앤매너를 레퍼런스로 반영할 수 있게 한다.

**입력 구조화 (표 형식, `lib/storyboardIdea.ts`)**
- 제목(짧은 텍스트, 필수) / 장르(프리셋 18종 태그 다중 선택 + "기타" 직접 입력 태그 추가, 1개 이상 필수) / 시청 타깃(짧은 텍스트, 필수) / 프로그램 개요(자유 서술형, 50~500자 필수 — 기존 "입력 글자 수 50~500자" 규칙이 이 필드로 옮겨졌다) 4행짜리 `<table>`로 화면에 표시한다.
- 장르 프리셋: 여행/음악/음식/관찰/스포츠/연애/두뇌게임/장사·경영/교양/페이크다큐/힐링/부동산/토크쇼/오디션/서바이벌/버라이어티/리얼리티/크로스오버 (`GENRE_PRESETS`). 프리셋 칩을 클릭하면 토글 선택되고, "기타" 입력창에 직접 쓴 태그는 Enter 또는 "추가" 버튼으로 별도 칩(제거 가능)으로 추가된다. 최대 10개까지.
- 화면(클라이언트)과 서버가 각각 4개 필드를 검증한 뒤, `composeIdeaText()`로 "제목: .../장르: .../시청 타깃: .../프로그램 개요: ..." 형태의 한 덩어리 텍스트로 합쳐 AI 프롬프트(`lib/storyboardPrompt.ts`)에 그대로 전달한다 — 프롬프트 조립 로직 자체는 기존과 동일하게 문자열 하나(`idea`)를 받는다.
- 프레임 재생성(`/api/storyboard/frame`) 요청에도 최초 생성 때와 동일한 4개 필드를 그대로 다시 실어 보내, 서버가 매번 같은 문구로 재조립한다(재생성마다 문구가 미묘하게 달라져 서사 맥락이 흔들리는 것을 방지).
- **기존 저장 데이터 호환**: localStorage에 예전 형식(`idea` 한 덩어리 문자열)으로 저장돼 있던 값은 "프로그램 개요" 필드로 그대로 옮겨 담아 복원한다 (제목/장르/시청 타깃은 예전 형식에 없던 값이라 빈 값에서 시작). 기존 작업 내용을 구조 변경만으로 통째로 잃지 않도록 하기 위함.

**레퍼런스 예능 (이름 + 이미지 → 톤앤매너 참고)**
- 예능 기획 아이디어 표 옆에 독립 박스로 배치 (`ReferenceShowInput.tsx`, `CharacterDescriber.tsx`와 같은 패턴). 레퍼런스 예능 이름(텍스트, 선택 입력) + 이미지 1장(JPG/PNG/WEBP, 최대 4MB) 업로드.
- "레퍼런스 분위기 분석" 클릭 → 이미지를 base64로 인코딩해 `POST /api/reference`로 전송 → 서버가 확장자·용량 재검증 → OpenAI 비전 모델에 "이 이미지의 연출 톤(색감·조명·구도·편집 리듬)만 묘사, 스토리·인물·옷차림은 언급하지 말 것"을 요청(자동 재시도 3회 + 형식 위반 시 1회 재요청, `/api/character`와 동일한 2계층 구조) → zod로 결과 형식(최소 40자) 검증 → 결과 텍스트만 화면에 표시.
- **이미지 원본은 저장하지 않는다** — 캐릭터 사진과 같은 원칙으로, localStorage에도 서버에도 남기지 않고 분석 API 호출 1회에만 쓰이고 사라진다. 이름(`referenceShowName`)과 AI가 만든 분위기 묘사 텍스트(`referenceDescription`)만 localStorage에 저장된다.
- 스토리보드 생성/재생성 프롬프트에 "레퍼런스 예능 참고" 블록으로 이름 + 분위기 묘사를 함께 전달하고, "그 프로그램의 스토리·소재·인물을 그대로 베끼지 말고 촬영 톤앤매너만 참고하라"고 명시적으로 지시한다(저작권·표절 우려 방지 목적의 프롬프트 가드레일).

## 9. 스타일/캐릭터/레이아웃/대사 확장 (PRD 최초 범위 밖, 사이클 진행 중 추가됨 — PLAN.md 26번)

**스타일 고정 자동 제안(그때그때 수정) / 네거티브 프롬프트 고정**
- 사용자 요청 — "스타일 고정과 네거티브 프롬프트를 스토리보드에 맞게 그때그때 수정해주게 해줘" → 실제로는 두 값의 요구가 서로 달라, 스타일 고정만 자동으로 바뀌고 네거티브 프롬프트는 반대로 고정한다.
- 스타일 고정: `POST /api/storyboard`가 12프레임과 함께 이번 기획 아이디어(장르·톤·레퍼런스)에 맞춘 새 styleLock 문구를 top-level로 함께 응답한다(`lib/storyboardSchema.ts`의 `storyboardGenerationSchema` — 기존 `storyboardSchema`에 styleLock 필드를 zod `.and()`로 덧붙임). 생성 성공 시 화면이 이 값으로 styleLock 상태를 자동 교체한다(PD가 이후 직접 수정 가능). 프레임 재생성은 styleLock을 새로 제안하지 않는다(전체 스토리보드 단위로만 갱신).
- 네거티브 프롬프트: 사용자가 준 고정 문구(`lib/styleLockDefaults.ts`의 `DEFAULT_NEGATIVE_PROMPT`)로 교체했고, 스토리보드를 생성/재생성해도 AI가 건드리지 않는다 — PD가 텍스트 영역에서 직접 고칠 때만 바뀐다.

**스타일 고정 생성 알고리즘 상세 (사용자 요청, PLAN.md 27번 — 장르 태그 기반 4그룹 매핑)**
- `lib/storyboardPrompt.ts`의 `STYLE_LOCK_GENERATION_RULES`가 시스템 프롬프트에 4개 시각 그룹(카메라 렌즈·조명·색감·무빙)을 정의해 AI에게 그대로 지시한다 — [감성/힐링](여행·힐링·관찰·음식·장사경영·교양·부동산: 35mm·얕은 심도·자연광·따뜻한 채도·필름 그레인·부드러운 핸드헬드) / [긴장/액션](서바이벌·두뇌게임·스포츠: 시네마틱 와이드·로우키 강한 명암비·틸앤오렌지·거친 질감·날것의 핸드헬드·더치 앵글) / [스튜디오/화사함](토크쇼·오디션·연애·음악: 소프트박스 조명·맑은 피부톤·파스텔/선명한 무대색·고정 앵글 또는 짐벌) / [방송 리얼리티](버라이어티·리얼리티·페이크다큐·크로스오버: 하이엔드 방송 카메라 룩·깊은 심도·쨍한 콘트라스트·깨끗한 해상도).
- 선택된 장르 태그가 여러 그룹에 걸치면 AI가 두 그룹의 시각 특징을 자연스럽게 융합하도록 지시한다(예: #서바이벌+#음식 → 어두운 로우키 속에서도 음식 채도만 강하게). 18종 프리셋에 없는 "기타" 직접 입력 태그(예: "직업 체험", "납량 특집")는 문자 그대로의 내용 설명이 아니라 그 컨셉에 어울리는 시각적 무드로 능동적으로 해석해서 반영하도록 지시한다.
- 이 로직은 **최초 생성(`buildStoryboardPrompt`)에만 적용**하고, 프레임 재생성(`buildFrameRegenPrompt`)에는 적용하지 않는다 — 최초 생성 프롬프트에서는 "이전 styleLock 초안을 참고하라"는 안내를 아예 빼서(예전에는 `buildStyleLockBlock`으로 함께 보냈음) 장르 매핑 규칙과 충돌하지 않게 했고, 프레임 재생성은 여전히 기존 styleLock을 컨텍스트로 받아 그 스타일을 유지한 채 그 프레임만 새로 쓴다.
- **영문 필수 방어 문구 (★사용자 강조 — "토씨 하나 틀림없이 무조건")**: `lib/storyboardExport.ts`의 `STYLE_LOCK_MANDATORY_SUFFIX` 상수(`"broadcast-safe exposure. No on-screen text, no graphics, no subtitles, no UI overlays. All signage kept strictly out of focus and non-legible. Photoreal, high-end broadcast quality, not CGI."`)를 AI 번역에 맡기지 않고 `buildFrameExportBlockEn`이 매 프레임 [IMAGE] 블록의 스타일 줄 끝에 코드로 그대로 붙인다 — styleLock이 비어 있어도 예외 없이 붙는다(실제로 AI 번역 결과를 확인해보니 의미는 비슷해도 문구가 매번 달라져, 정확한 재현이 필요한 이 문장만은 코드로 고정하는 것이 유일하게 안전한 방법이었다).
- 출력 형식(한국어 UI/영문 Export 분리)은 기존 아키텍처를 그대로 따른다 — 생성 API는 한국어 styleLock만 돌려주고(화면에 표시·편집), 영문은 내보내기(export-translate) 시점에 번역 + 위 방어 문구 코드 부착으로 만들어진다. 매 생성마다 한국어·영문을 동시에 만들면 OpenAI 호출이 2배로 늘어 비효율적이라, 기존 "내보낼 때만 번역" 구조를 재사용하는 쪽으로 판단했다.

**대사(dialogue)/자막(caption) 필드**
- 사용자 요청 — "스토리보드 생성시 대사도 추가, 캐릭터가 한국 사람이면 한국말, 외국 사람이면 영어, 대사에 맞는 자막도 추가". 프레임에 `dialogue`(대사)/`caption`(자막) 필드를 추가한다(`lib/storyboardSchema.ts`) — 대사가 있는 장면만 채우고(리액션 샷 등은 둘 다 빈 문자열), zod로 "대사가 있으면 자막도 있어야 한다" 짝을 검증한다.
- 언어 규칙(`lib/storyboardPrompt.ts`의 `DIALOGUE_RULES`, 최초 생성/재생성 공통): 기획 아이디어·고정 출연자 설명에 외국인/외국어권 설정이 있으면 그 인물의 dialogue는 영어로, 한국인이면 한국어로 쓴다. caption은 항상 한국 방송 자막(한국어) — dialogue가 영어면 caption에 한국어 번역을 적는다. 실제 호출로 "한국인 셰프+미국인 셰프" 시나리오를 검증해, 미국인 셰프 대사는 영어(dialogue)+한국어 자막(caption)으로, 한국인 셰프 대사는 한국어로 정확히 갈렸다.
- dialogue/caption은 [IMAGE]/[VIDEO] 시각 프롬프트에는 절대 섞이지 않는다(기존 "읽는 글자 금지"·"립싱크 실패 방지" 렌더 안정성 규칙과 충돌하지 않도록) — export-translate로 번역하지도 않는다(이미 화자 국적에 맞는 언어로 생성됐고, caption은 한국어 고정이라 재번역이 목적에 어긋남). 대신 `lib/storyboardExport.ts`가 프레임 블록의 `[AUDIO]`와 `[NEGATIVE PROMPT]` 사이에 `[DIALOGUE]` 블록으로 원본 그대로 덧붙이며, "영상 생성 프롬프트에 텍스트로 노출하지 말 것, 별도 더빙·자막 작업에 사용"이라는 안내 문구를 함께 넣는다.
- FrameCard에는 대사/자막이 있을 때만 "대사: .../자막: ..." 줄을 보여준다. 편집 UI는 없음(AI가 생성한 값 그대로).

**캐릭터 4/5 추가**
- 사용자 요청으로 `lib/castCharacters.ts`의 `CAST_CHARACTERS`를 3개(캐릭터1~3) → 5개(캐릭터1~5)로 확장. 캐릭터4/5의 role은 각각 "추가 출연자2"/"추가 출연자3"(캐릭터3은 "추가 출연자1"로 통일). `DEFAULT_CAST_IDS`(자동 태깅 대상)는 캐릭터1/2로 변경 없음 — 3/4/5는 전부 PD가 수동으로 태깅한다.

**헤딩 색상·위치 통일 (PLAN.md 28번)**
- 사용자 요청 — "레퍼런스 예능/캐릭터 생성도 예능 기획 아이디어처럼 검은색 폰트로". `ReferenceShowInput.tsx`/`CharacterDescriber.tsx`의 제목(`<h2>`)에서 기존 `text-ena-blue` 색상 클래스를 제거해, "예능 기획 아이디어" 제목과 같은 기본(검은색) 볼드로 통일했다.
- 사용자 요청(2차) — "캐릭터/레퍼런스 예능 제목을 네모 박스 안이 아니라 위에 왼쪽 정렬로". 두 제목 모두 박스 내부에서 `StoryboardWorkspace.tsx`(부모)로 옮겨, "예능 기획 아이디어"와 동일하게 박스 바깥 위쪽에 표시한다. 캐릭터는 5개 박스 전체를 대표하는 "캐릭터 생성" 제목 하나만 그리드 위에 두고, 각 박스 안의 개별 제목은 중복을 피하기 위해 "카릭터N 설명 생성" → "카릭터N"으로 줄였다.
- 사용자 요청(2차) — "레퍼런스 예능 박스와 예능 기획 아이디어 박스 높이 맞추기". 두 박스를 감싸는 행의 정렬을 `items-start`(위쪽 정렬만) → `items-stretch`(같은 높이로 늘림)로 바꾸고, 레퍼런스 예능 쪽을 `flex flex-col`로 감싸 늘어난 높이를 `ReferenceShowInput.tsx`(`flex-1`)가 채우도록 했다 — 내용이 짧아 아래쪽에 여백이 생기더라도, 박스 테두리 높이 자체는 예능 기획 아이디어 박스와 항상 같아진다.

**출연진 역할 입력 (PLAN.md 29번)**
- 사용자 요청(3차) — "프로그램 개요 위에 생성된 캐릭터들에 대한 역할을 작성할 수 있게 해줘". 예능 기획 아이디어 표에 "시청 타깃"과 "프로그램 개요" 사이 "출연진 역할" 행을 추가. 설명이 이미 생성된 캐릭터만 골라 라벨+입력창을 보여준다(하나도 없으면 안내 문구만).
- `characterRoles`(캐릭터id → 역할 문자열, 최대 40자 `ROLE_MAX_LENGTH`) 상태를 `StoryboardWorkspace.tsx`에 신설하고 localStorage(`lib/storyboardStorage.ts`)에 함께 저장/복원한다.
- `resolveCastMembers`가 프롬프트용 `CastMemberForPrompt.role`을 만들 때, `characterRoles[id]`가 있으면 `CAST_CHARACTERS`의 고정 role("진행자"/"고정 파트너" 등)보다 우선 사용한다(비어 있으면 기존 고정 role 그대로) — 스토리보드 생성/재생성 프롬프트(`lib/storyboardPrompt.ts`)의 "고정 출연자 외형 설명" 블록과 내보내기(`lib/storyboardExport.ts`)의 캐스트 라인에 그대로 반영된다. 서버(`app/api/storyboard(/frame)/route.ts`)는 이미 role을 임의 문자열로 검증해 받고 있어 zod 스키마 변경은 불필요했다.

**레이아웃: 4x3 그리드 + 가로길이 균일화**
- 사용자 요청 — "1~12번 스토리보드는 4x3배열로, 캐릭터 설명/예능 기획 아이디어+레퍼런스 예능/스토리보드 전체 가로길이를 균일하게". 12프레임 그리드를 `grid-cols-3`(3열×4행) → `grid-cols-4`(4열×3행)로 변경.
- 세 구역(캐릭터 행, 아이디어+레퍼런스 행, 12프레임 그리드, 내보내기 버튼 행) 모두 같은 `max-w-[1720px]` 컨테이너 폭을 쓰도록 통일했다. 캐릭터 행은 `grid grid-cols-5`로 5칸을 균등 분할(개별 박스의 `max-w-xs` 상한 제거), 아이디어+레퍼런스 행은 기존에 폭을 좁히던 `max-w-[1200px]`를 없애고 아이디어 표를 `flex-1`로 늘려 레퍼런스 박스(고정폭) 옆 남는 공간을 채운다. 프레임 그리드·내보내기 행은 기존 `max-w-5xl`(896px)이었던 것을 `max-w-[1720px]`로 넓혔다.

## 10. 발화 묘사 제거 및 비언어적 표현 자동 치환 (PRD 최초 범위 밖, 사이클 진행 중 추가됨 — PLAN.md 31번)

목표: 구글 Flow Omni 같은 영상 생성 AI는 인물이 "말하는" 것처럼 묘사되면 립싱크를 시도하다가 얼굴 픽셀이 기괴하게 뭉개지는 치명적 렌더 오류를 낸다. PD가 씬에 구체적인 대사·자막을 적더라도, 구글 Flow Omni로 나가는 최종 영어 텍스트에는 그 내용이 발화 행위가 아니라 안전하게 렌더링되는 표정·제스처·비언어적 소리로만 도달하게 한다.

**적용 지점 — 2단계 방어**
- **1단계(생성, 한국어)**: `lib/storyboardPrompt.ts`의 `RENDER_SAFETY_RULES`(최초 생성·재생성 공통)에 "imageDescription/videoAction에는 '말한다'/'대화한다'/'외친다' 같은 발화 행위 자체를 묘사하지 않고, 그 감정에 맞는 표정·제스처로 바꿔 쓴다"는 규칙과 치환 예시(오랜만이야→손 흔들며 웃음, 너무 맵다→인상 찌푸리고 부채질, 즐겁게 대화→마주보고 박수치며 웃음)를 추가했다. audioNote 규칙에도 "가벼운 웃음소리/호탕한 웃음/놀란 숨소리/알아들을 수 없는 기분 좋은 웅성거림" 같은 비언어적 소리 어휘를 추가했고, "화면에 자막을 띄워달라는 요청도 반영하지 않는다"는 규칙도 추가했다.
- **2단계(번역, 영어) — 실제 사고 지점의 마지막 방어선**: `lib/exportTranslatePrompt.ts`의 번역 시스템 프롬프트가 가장 중요하다. 실제로 구글 Flow Omni에 들어가는 텍스트는 이 번역 단계의 출력이기 때문이다. 규칙 1(의미 그대로 번역)보다 규칙 2·3(안전 치환)이 우선한다고 명시하고, `talk/talks/talking/talked/speak/speaks/speaking/spoke/spoken/say/says/saying/said/dialogue/conversation/chat/chats/chatting` 등 발화 관련 영어 단어를 절대 쓰지 않도록 지시한다. 원문이 대사를 담고 있어도 "번역"이 아니라 "안전한 재구성"으로 취급해, 없던 표정·동작을 새로 만들어 붙이는 것도 허용한다(사용자가 준 치환 예시 3개를 프롬프트에 그대로 포함). audioNote 번역 규칙도 동일하게 비언어적 소리(light laughter, hearty chuckle, surprised gasp, unintelligible warm murmurs)로만 표현하도록 지시하고, 화면 자막 요청은 번역하지 않고 생략하도록 지시한다.

**코드 검증 — 정규식 기반 형식 재요청 (프롬프트 지시만으로는 "예외 없음"을 보장할 수 없다는 걸 이 세션에서 반복 확인했기 때문)**
- `lib/exportTranslateSchema.ts`에 `superRefine`을 추가해, 번역된 각 프레임의 imageDescription/videoAction에 발화 관련 영어 단어(위 목록)가 하나라도 남아 있으면 zod 검증을 실패시킨다. `storyboardSchema.ts`의 "8초 이상 비트 분할 강제"와 동일한 패턴 — 검증 실패 시 `app/api/storyboard/export-translate/route.ts`의 기존 "형식 위반 시 동일 프롬프트로 1회 재요청" 루프가 그대로 재사용된다(새 API·새 재시도 로직을 만들지 않음). styleLock/negativePrompt/label/role은 검사 대상에서 제외했다 — "토크쇼" 장르가 "talk show"로 번역되는 등 정당한 사용이 있어, 인물의 발화 행위를 직접 묘사하는 imageDescription/videoAction에만 좁혀 적용한다.
- 실제 curl 테스트로 확인 — "오랜만이야!"라고 인사하며 대화를 나누는 한국어 원문을 번역했을 때, AI가 실제로 "Waves enthusiastically", "looking at each other, nodding and laughing while clapping"처럼 talk/speak/say를 전혀 쓰지 않고 표정·제스처로 재구성한 결과를 1회 만에 반환하는 것을 확인했다.

**audioNote 필수 접미사 — 코드 고정 (AI 번역 신뢰 대신 결정적 부착)**
- `lib/storyboardExport.ts`에 `AUDIO_NO_DIALOGUE_SUFFIX = "No intelligible dialogue."` 상수를 추가하고, `buildFrameExportBlockEn`이 모든 프레임의 `[AUDIO]` 줄 끝에 번역 결과와 무관하게 항상 이 문자열 그대로를 붙인다(audioNote가 비어 있어도 예외 없이) — `STYLE_LOCK_MANDATORY_SUFFIX`와 정확히 같은 이유·같은 패턴이다(AI 번역에 맡기면 문구가 매번 미묘하게 달라질 수 있다는 걸 이 세션 앞부분에서 실측으로 이미 확인했음).

**[DIALOGUE] 블록 100% 제거 — 내보내기는 4블록으로 고정**
- `buildFrameExportBlockEn`에서 dialogue/caption 매개변수와 `[DIALOGUE]` 블록 조립 로직을 완전히 제거했다. 이제 이 함수의 최종 출력은 항상 `[IMAGE]`/`[VIDEO - Ns]`/`[AUDIO]`/(있으면) `[NEGATIVE PROMPT]` 4블록으로만 구성된다. `ExportFrameContext` 타입과 `StoryboardWorkspace.tsx`의 `buildFrameContexts`/`handleCopyFrame` 호출부에서도 dialogue/caption 전달을 제거했다.
- dialogue/caption 필드 자체(스키마·생성·저장)는 유지한다 — PD가 화면(`FrameCard.tsx`)에서 "이 장면에서 인물이 대략 무슨 말을 하는지" 참고하는 용도로는 여전히 값을 만들어 보여준다. 다만 이제 "구글 Flow Omni로 나가는 값이 아니라 화면 참고용일 뿐"이라는 안내 문구를 대사 옆에 표시해 PD가 오해하지 않도록 했다.

## 11. AI 형식 재요청 신뢰성 강화 — 공용 재시도 유틸 (PRD 최초 범위 밖, 사이클 진행 중 추가됨 — PLAN.md 32번)

목표: "AI 응답이 12프레임 형식 규칙을 지키지 않았습니다"(`AI_FORMAT_ERROR`) 오류가 가끔 발생하는 문제를 줄인다. 사용자 요청 — "무조건 12프레임 나오도록 강제해줘".

**실측으로 확인한 진짜 원인**: 디버그 로그를 직접 남겨 확인한 결과, 실패 원인은 "프레임이 12개가 아님"이 아니라 "12개 중 1~2개 프레임의 imageDescription이 100자를 살짝 못 채움" 또는 "8초 이상 프레임에 '비트 1 (0~4초): ...' 형식이 빠짐" 같은 부분적 규칙 위반이 대부분이었다. 기존 구현은 이럴 때도 완전히 같은 프롬프트로 그대로 재요청해서, 같은 실수를 반복할 확률이 낮지 않았다.

**신설한 공용 유틸 — `lib/aiJsonRetry.ts`(`requestJsonWithRetry`)**: `/api/storyboard`, `/api/storyboard/frame`, `/api/storyboard/export-translate` 3개 라우트가 각자 복붙해서 갖고 있던 "AI 호출 → JSON 파싱 → zod 검증, 실패 시 형식 재요청" 루프를 한 곳으로 모았다.
- **핵심 개선 — 실패 원인 피드백**: 형식 위반 시 실패한 응답(assistant 메시지)과 zod가 잡아낸 구체적인 문제 목록("frames.3.imageDescription: 100자 이상이어야 합니다" 같은 항목 최대 12개)을 대화 기록에 남겨서 "이 부분만 정확히 고쳐서 완전한 JSON 전체를 다시 보내라"고 요청한다. 실제 curl 테스트로 문제 8개(짧은 imageDescription 5개 + 비트 분할 누락 3개)가 재요청 1회 만에 1개로 줄어드는 것을 확인했다.
- `max_tokens`를 12000으로 넉넉히 고정해 12프레임 분량 응답이 도중에 잘려 JSON 자체가 깨지는 경우를 방지한다(실제 비용은 사용한 토큰만큼만 청구되어 상한을 넉넉히 잡아도 손해가 없다).
- 프롬프트(`lib/storyboardPrompt.ts`)의 "정확히 12개" 규칙과 "imageDescription 100자 이상" 규칙에도 "★필수, 절대 예외 없음" 강조와 "응답 전 스스로 개수·글자 수를 세어 확인"하는 자기 검토 지시를 추가했다(코드 검증과 별개로, 애초에 위반이 덜 나오도록 하는 1차 방어).

**재시도 횟수는 의도적으로 2회로 유지 — Vercel `maxDuration`(60초) 제약과의 트레이드오프**: 재시도를 3회로 늘려 실측한 결과, 매 시도가 실제 OpenAI 호출 1회(약 20~60초, 편차 큼)라서 3회를 다 채우면 총 소요 시간이 로컬 테스트에서 100초 이상 걸리는 사례를 직접 확인했다 — Vercel에 배포하면 `maxDuration=60`을 넘겨 타임아웃으로 실패할 위험이 있다. 사용자에게 "재시도 3회+시간제한 확대(Vercel Pro 이상 필요)" vs "현재 2회 유지" 중 선택하도록 물었고, **"현재 상태 유지"**로 결정했다 — 그래서 `requestJsonWithRetry`의 기본 `maxAttempts`는 2회다. 다만 프레임 재생성(`/api/storyboard/frame`)은 응답이 프레임 1개뿐이라 훨씬 가볍고 빠르므로, 그 호출부에서만 `maxAttempts: 3`으로 개별적으로 올려서 안전 여유 안에서 신뢰성을 조금 더 높였다.

**한계 (정직하게 남겨둠)**: 이 개선은 실패 확률을 크게 낮추지만, AI 생성 결과의 창의적 변동성상 100% 보장은 불가능하다. 2회 모두 실패하면 기존과 동일하게 `AI_FORMAT_ERROR` + 화면의 수동 "재시도" 버튼으로 안내된다(공통 규칙, PRD 5절).

## 참고 문서

- [PLAN.md](PLAN.md) — 작업 순서 계획
- [PRD.md](PRD.md) — 전체 기획서
- [CLAUDE.md](CLAUDE.md) — 작업 규칙 및 핵심 비즈니스 규칙
